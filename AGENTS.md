# 作業の継続と記録

このプロジェクトで作業するエージェントは、最初に `WORK_LOG.md` を読む。

## 再開手順

1. 現在の作業ディレクトリとファイルの実体を確認する。
2. `git rev-parse --is-inside-work-tree` でGit管理の有無を確認する。Git管理下なら `git status --short`、`git diff`、`git diff --cached` を確認し、未追跡の関連ファイルも読む。Git管理外ならその事実を記録し、差分がないとは扱わない。
3. `WORK_LOG.md` の現在地・次の作業・検証結果と、対象ファイルの内容を照合する。会話の記憶や記録だけで完了を判断しない。ユーザーの変更を上書きしない。
4. 実行途中の処理があれば、記録されたセッション・プロセスや成果物を確認してから判断する。タイムアウトだけを理由に重複実行しない。
5. 確認できた中断地点から継続する。未達のGoalがあれば、その全成功条件を満たすまで継続する。未完了を完了に書き換えない。

## 記録の更新

`WORK_LOG.md` を次の単位で更新する：作業開始時、まとまった変更の完了時、検証完了時、問題・設計判断の発生時、作業終了前。中断が予測できない場合にも直近の状態を復元できるよう、最後にまとめて記録する運用にしない。

常に以下を明記する：

- 完了した作業と対象ファイル
- 現在作業中の項目と途中状態
- 未完了の項目と成功条件
- 次に実行すべき具体的な作業・コマンド
- 実行済みのテスト、その結果、検証していない範囲
- 発見した問題と対処状況
- 重要な設計判断と理由

先頭の現在地は最新に保ち、履歴は日付を付けて追記する。失敗したコマンド、承認拒否、残存する制約も隠さず記録する。秘密情報は記録しない。

## 編集対象

- ガルパ楽曲データ：`data/garupa/songs.json`（`groups` 内の曲）
- アワーノーツ楽曲データ：`data/ournotes/songs.json`
- 画面・検索・並べ替え：`src/`
- 新曲ひな型：`templates/song.json`
- `dist/` は生成物。直接修正せず、`node scripts/build.mjs` で反映する。
- 操作手順は `README.md` と `templates/README.md` を参照する。
